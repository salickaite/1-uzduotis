#pragma once

#include "studentas.h"
#include <vector>
#include <list>
#include <string>
#include <fstream>
#include <iostream>
#include <iomanip>

void v_write(std::vector<studentas>& v, std::string pav);

void l_write(std::list<studentas>& l, std::string pav);